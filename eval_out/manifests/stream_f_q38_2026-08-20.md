# Stream F addendum — Qwen3.8-27B-4bit arm (2026-08-20)

**Why:** `mlx-community/Qwen3.8-27B-4bit` was found in the laptop's HF cache, downloaded
but never benchmarked — the "audit the defaults" pattern applied to the reader. One new
arm on the existing stream-F runner (`eval_out/stream_f_model_ab.py`), same seeded FUNSD
n=50, identical items, live MLX server, noise floor zero.

**Caveat:** the hand-labelled set is missing (lost in the 2026-08-17 storage shuffle —
see the engine_ab manifest), so this arm has **FUNSD rows only**; the runner now warns
loudly and skips labels instead of crashing. Compare per-set — the printed `label`/`all`
deltas for this arm are artifacts of the empty label set; the honest row is FUNSD.

## Result (FUNSD n=50, identical items)

| model | recall | precision | microCER | medCER | insertion | runaway | t_vlm |
| --- | --- | --- | --- | --- | --- | --- | --- |
| q36_35ba3b_4bit (default) | **0.8317** | **0.8712** | **0.1415** | **0.1011** | **0.0641** | 0 | **13.31s** |
| q38_27b_4bit | 0.8258 | 0.8615 | 0.1540 | 0.1059 | 0.0691 | 0 | 18.05s |

**Verdict: keep Qwen3.6-35B-A3B.** The dense 27B loses on every metric AND is ~36%
slower — the 3B-active MoE beats it on both axes (recall −0.006, medCER +0.005 for 3.8;
no runaways either side). The cached 3.8-27B earns no place as generalist or escalation
tier; it can be evicted from the cache (~15 GB) at will. Rollback lineage unchanged:
Qwen3.6-35B-A3B default, Qwen3.5-9B-4bit rollback.

---
*Correction (2026-08-20, same day): the missing-labelset caveat was a false alarm — the
labelset lived on the desktop under a differently-named repo path and is now recovered onto
both machines. This arm's FUNSD-only scope stands (the arm ran during the alarm window);
its label rows can be back-filled by re-running the runner if ever needed.*
